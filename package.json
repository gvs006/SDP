{
  "name": "mei-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/sync-env.js && turbo run dev",
    "build": "node scripts/sync-env.js && turbo run build",
    "lint": "turbo run lint",
    "build:all": "pnpm build"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
